export const metadata = {
  title: "Scenario Comparison — Kairos",
  description:
    "Compare up to 4 patient risk profiles side-by-side with overlaid stillbirth risk curves.",
};

export default function ScenariosLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
